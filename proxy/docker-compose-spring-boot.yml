version: "3"

services:
  petstore:
    image: networknt/springboot-petstore
    network_mode: host
  proxy:
    image: networknt/light-proxy
    volumes:
    - ./config/http:/config
    ports:
    - 8000:8000
    network_mode: host
