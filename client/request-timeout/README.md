To test this live, please start the opeanapi-petstore server with the following commands.

```
cd ~/networknt
git clone https://github.com/networknt/openapi-petstore.git
cd openapi-petstore
mvn clean install exec:exec
```

