To test this live, please start the http server with the following commands.

```
cd ~/networknt
git clone git@github.com:networknt/http2client-benchmark.git
cd http2client-benchmark/httpserver
mvn clean install exec:exec
```
